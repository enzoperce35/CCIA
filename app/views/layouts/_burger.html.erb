<div class="hamburger">
  <span class="bar"></span>
  <span class="bar"></span>
  <span class="bar"></span>
</div>

<div>
  <ul class='nav-menu'>
    <ul class='nav-item'>
      <% if @selected_coin.present? %>
        <% coin = user( @selected_coin) %>
        <%= link_to is_user_owned?(coin) ? 'Unmark Coin': 'Mark Coin', coin_path(coin), method: 'patch' %><br>
        <%= link_to 'Delete Coin', coin_path(coin), method: 'delete', data: { confirm: "Are you sure you want to delete #{@selected_coin} ?"} %><br>
      <% end %>
      
      <%= link_to 'Add Coin', new_coin_path, method: 'get' %><br>
      <%= link_to 'Observe Mode', root_path(observe: true), method: 'get' %><br>
      <%= link_to 'Sign_out', destroy_user_session_path, method: 'delete' %>
    </ul>
  </ul>
</div>
