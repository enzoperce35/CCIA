<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <div>
      <%= link_to  'Coins', root_path( actives: true ), {:class=>"navbar-brand", method: 'get'} %>
    </div>

    <div id= "main-menu">
      <% unless Coin.count.zero? %>
        <%= render 'layouts/burger' %>
      <% end %>
    </div>  
  </div>
</nav>
