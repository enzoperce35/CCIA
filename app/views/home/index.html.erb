<%= link_to 'Sign_out', destroy_user_session_path, method: 'delete' %>
<%= link_to 'Trade', root_path(trade: true), method: 'get' %>
<%= link_to 'Observe Coin', new_coin_path(status: 'observe'), method: 'get' %>
<%= link_to 'Buy Coin', new_coin_path(status: 'buy'), method: 'get' %>

<script>
  data_arr = []
  id_arr = []

  function render_data(data_arr, id_arr) {
  data_arr.forEach((data, index) => {
    const id = id_arr[index]
   
     document.getElementById("prog-" + id).style.width = data;
    });
  };
</script>

<% if @trade %>
  <%= render 'trading_view' %>
<% else %>
  <%= render 'coin_view' %>
<% end %>

<script>
  window.setInterval(function () {
      if (counter === 0) {
          clearInterval(counter);
      }

  }, 1000);

  window.setInterval('refresh()', 30000);

  function refresh() {
      window  .location.reload();
  }
</script>
